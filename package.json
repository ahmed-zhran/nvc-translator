{
  "scripts": {
    "start:android": "npx cap sync && npx cap open android",
    "start:ios": "npx cap sync && npx cap open ios",
    "start:web": "python -m flask run",
    "build": "npm i && pip install -r requirements.txt && python app.py && if not exist android (npx cap add android) && if not exist ios (npx cap add ios) && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.6.0",
    "@capacitor/ios": "^5.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.6.0",
    "@capacitor/core": "^5.6.0"
  }
}
